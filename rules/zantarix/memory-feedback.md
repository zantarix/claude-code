# Memory: generalize feedback entries

Write feedback memories as generalized, session-agnostic rules. When an entry mixes a one-off instance with a generalizable principle, rewrite it around the principle and strip the instance — mention the originating instance only as a brief illustration. Do not delete the whole entry just because the instance is over; delete outright only when the entry is purely descriptive of code or state that now exists.
