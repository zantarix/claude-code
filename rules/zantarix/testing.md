# Test requirements

Tests must encode WHY behavior matters, not just WHAT it does. A test that can't fail when business logic changes is wrong.

Unit tests should read and act as a specification for the behaviour it is testing.

Prefer unit tests for coverage. Prefer integration tests for testing user workflows. Both are necessary.
