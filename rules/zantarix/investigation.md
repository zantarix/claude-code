# Investigating failures

When a tool or test fails unexpectedly, do not call it a "known issue" or "toolchain bug" without a source (link, issue number, or reproduction steps). First test without your changes to establish a baseline: if it only fails after your changes, it is a regression you introduced. Never use vague attribution to move past a failure.
