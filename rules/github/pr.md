When the user asks to create/open/raise a pull request on a GitHub project ("create a pr", "open a pull request", "raise a PR", or similar), invoke the `github:create-pull-request` skill via the Skill tool before any other action — do not reach for `gh pr create` directly. Follow the skill's workflow once invoked.
