Link related GitLab work items rather than describing relationships in prose (see `terminology`) — prose like "blocked by #N" is invisible to the board, the work-item graph, and the "Linked items" widget. Whenever a description would say "blocks #N", "blocked by #N", "related to #N", or "see #N", create the link by invoking the `gitlab:link-work-items` skill via the Skill tool (it carries the MCP/GraphQL incantation, the uppercase `linkType` enum, and the iid→GID resolution). Keep prose explanation if it adds context, but the formal link is canonical.

See also: `prefer-gitlab-mcp`, `status`.
