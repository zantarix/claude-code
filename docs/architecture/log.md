# Log

## 2026-08-13

* **Update**: numbered the decision entries in all three theme indexes — an ADR's bullet now reads `[0007 - Title]`, four-digit padded to match its filename, so a reader scanning an index can cite a decision without opening it. Living documents keep bare titles, and the root index has no per-decision roll-call to number. The convention is stated in the `okf-guide` architecture-bundle profile, with pointers from `/okf-index` and `/okf-curate` so a bulk reindex does not strip the numbers back off.
* **Acceptance**: marked [ADR-0007](knowledge/0007-track-specification-conformance-in-status.md) and [ADR-0008](components/0008-keep-judgment-correcting-content-out-of-on-demand-skills.md) Accepted — both ratified by `human:mscharley`. Both are constraint admissions, so the [ledger](constraints.md) gained an entry for each in the same operation: what a specification's `status` asserts about the contract, and that judgment-correcting content is loaded by something other than the judgment it corrects. ADR-0007 was amended at the gate on the human's direction — the Neutral bullet deferring the [ADR-0004](knowledge/0004-split-architecture-documentation-into-immutable-adrs-and-living-documents.md) erratum was deleted and the erratum written now, since [Maintaining Living Documents](knowledge/living-documents.md) has carried the reading since 2026-08-12 and ADR-0004's "untracked by design" record has been false since then rather than from ratification. That erratum names both sites carrying the claim and records that the scope exclusion beside them — that ADR-0004 declined to decide the question — stands. The sweep found nothing else affected: ADR-0002's preloading line sits inside a rejected alternative, and the preloaded homes ADR-0002 and ADR-0004 rely on are permitted by ADR-0008 rather than foreclosed by it. ADR-0007 is ratified ahead of its implementation: no distributed prompt yet carries the reading, so the [knowledge overview](knowledge/overview.md) now records that as decided-but-unbuilt rather than in flight, with the prompts out of conformance against a specification that stays `stable`. A gap on the implementation side is a defect to close, never grounds to relabel the document stating the contract.
* **Creation**: proposed [ADR-0008](components/0008-keep-judgment-correcting-content-out-of-on-demand-skills.md) — admits the rule the [components overview](components/overview.md) had been asserting unadmitted since 2026-08-12: content that exists to correct an agent's in-the-moment judgment is loaded by something other than that judgment, so prompt size is never grounds for moving it behind a skill the agent must decide to invoke. Backfill in [ADR-0006](knowledge/0006-require-a-curator-session-for-adr-ratification.md)'s shape — the practice is already in force and nothing is built by accepting it. The pre-acceptance review narrowed the carve-out: the overview's original "the trigger *is* the task" licensed exactly the split being foreclosed, since the task reaches the agent as a claim from its caller, so what may still move is material fired by something checkable rather than recognised — and the overview now says so, attributing the rule instead of establishing it while keeping the current-truth facts (the curator's two thirds, and that the apparatus does not fire on the common living-document case). Ledger entry follows at acceptance.
* **Creation**: proposed [ADR-0007](knowledge/0007-track-specification-conformance-in-status.md) — a specification's `status` answers the format's lifecycle question about the contract, narrowing the general meaning rather than replacing it since a contract the code does not meet is not settled either. It is set explicitly because the format's default otherwise claims conformance, and is promoted to `stable` with a `verified` event when the implementation lands. This answers a question [ADR-0004](knowledge/0004-split-architecture-documentation-into-immutable-adrs-and-living-documents.md) parked as out of scope, and forecloses describing a specification's editorial maturity through `status` inside an architecture bundle.
* **Update**: cited the driving decision from the status section of [Maintaining Living Documents](knowledge/living-documents.md), scoped the reading from any living document to specifications alone, gave `deprecated` a row, and replaced one-way promotion with the rule that the value follows the contract: a specification returns to `draft` only where a decision authorised a change it has not caught up to, never to absorb a divergence. Its own `status` stays `stable`.
* **Update**: recorded in the [knowledge overview](knowledge/overview.md) that the which-side-yields rule assigns fault on every divergence and therefore needs the expected direction readable from the document, and named ADR-0007 as the theme's one decision in flight, precise about what carries it: the bundle states the reading, and no prompt enacts it.
* **Update**: corrected what separates a specification from an overview in [Maintaining Living Documents](knowledge/living-documents.md) — the separator is **scope** (one named subject in depth, against a theme's whole orientation), and the citation requirement binding specifications only is a consequence of that scope rather than the test for it. The previous wording made binding the criterion, which contradicted the distributed [architecture vocabulary](../../plugins/zantarix/skills/architecture-vocabulary/SKILL.md).
* **Update**: thinned the ADR lifecycle section of the [knowledge overview](knowledge/overview.md) to orientation depth — who drafts, that review happens, that ratification is human, and that acceptance freezes the document — and left the field-level contract to the [ADR lifecycle](knowledge/adr-lifecycle.md) alone. The restated mechanics had already drifted once.
* **Update**: relocated the [ticket-to-merge workflow](ticket-workflow.md) from `components/` to the bundle root. Its subject runs across skills, rules, and several plugins, and the only decision behind it is an orchestration one, so no theme owned it; the root index now carries it as a cross-cutting specification.
* **Update**: recorded the third meaning of `disable-model-invocation` in the [orchestration overview](orchestration/overview.md) and the [components overview](components/overview.md) — reference prose nobody invokes, kept off every agent's skill listing because it exists to be preloaded. Preloading alone does not call for the flag: `okf-guide` and `materiality-gate` are preloaded and still invoked on demand elsewhere.
* **Update**: dropped the half-settled acceptance-discrepancy gap from the [orchestration overview](orchestration/overview.md). [ADR-0005](knowledge/0005-ratify-adrs-in-concert-with-the-human.md) settled it and the [ADR lifecycle](knowledge/adr-lifecycle.md) carries the sequence, so the entry was describing a question that is closed.

## 2026-08-12

* **Update**: swept the retired terms out of the bundle's living tier — "commitment" from the [ledger](constraints.md), both index one-liners that carried it, the [knowledge overview](knowledge/overview.md), and the [ticket-to-merge workflow](components/ticket-workflow.md), and "current-state material" from the [ADR lifecycle](knowledge/adr-lifecycle.md) — so the corpus the [architecture vocabulary](../../plugins/zantarix/skills/architecture-vocabulary/SKILL.md) governs uses "decision", "constraint", and "current truth" where each is meant. Accepted ADRs keep the old wording; it is a historical record.
* **Update**: restated the acceptance sweep's ledger test in the [ADR lifecycle](knowledge/adr-lifecycle.md) as foreclosure rather than as a constraint-admission/topology binary. The subject of a decision is description, not a category the taxonomy turns on: [ADR-0005](knowledge/0005-ratify-adrs-in-concert-with-the-human.md) admitted no entry because it relaxes a foreclosure, not because of what it was about.
* **Creation**: established [Maintaining Living Documents](knowledge/living-documents.md) — the citation requirement [ADR-0004](knowledge/0004-split-architecture-documentation-into-immutable-adrs-and-living-documents.md) placed on specification edits is what decides which side yields when a document and the code disagree, and it binds specifications only, since an overview follows reality by definition. Also fixes what `status` asserts: `draft` expects a gap, `stable` calls one a defect, and the format's `stable` default silently claims conformance for any specification written ahead of its implementation and left unmarked.
* **Creation**: established the distributed [architecture vocabulary](../../plugins/zantarix/skills/architecture-vocabulary/SKILL.md) as a preloaded, non-invocable skill so consumer projects receive the regime's definitions rather than only its consistent usage. Retires "commitment", which had been used for both a decision and a constraint.
* **Update**: recorded in the [components overview](components/overview.md) the limit on decomposing a prompt into on-demand skills — content that exists to correct in-the-moment judgment cannot be gated behind in-the-moment judgment, since an agent reliable enough to know it needs such a rule did not need it. Written after measuring the curator prompt against the preloading test and declining to split its authoring apparatus, which is roughly two thirds of it.
* **Acceptance**: marked [ADR-0005](knowledge/0005-ratify-adrs-in-concert-with-the-human.md) and [ADR-0006](knowledge/0006-require-a-curator-session-for-adr-ratification.md) Accepted — both ratified by `human:mscharley`. ADR-0006 is a constraint admission and its [ledger entry](constraints.md) was written in the same operation; ADR-0005 relaxes a foreclosure rather than creating one, so it admits none. The sweep of prior accepted ADRs found one affected: [ADR-0004](knowledge/0004-split-architecture-documentation-into-immutable-adrs-and-living-documents.md) received the erratum its "and nothing else" clause was owed, scoped to that half and recording that the guarantee around it survives. ADR-0001/0002/0003 were unaffected, and ADR-0006 falsifies nothing in ADR-0004 — adding the session requirement narrows "run by a person" without contradicting it.
* **Update**: implemented [ADR-0005](knowledge/0005-ratify-adrs-in-concert-with-the-human.md) in `/accept-adr`, which now carries the four-step amendment sequence from the [ADR lifecycle](knowledge/adr-lifecycle.md) in place of the freeze. The sweep for residual pre-decision framing found it restated on two further surfaces — the curator agent's regime section and the `adr` rule — both of which asserted that acceptance never touches the body; all three now state the guarantee as the text the human reads. The [knowledge overview](knowledge/overview.md) follows the code and records no open gap.
* **Creation**: established the [orchestration overview](orchestration/overview.md) — the bundle's first living document, describing how multi-agent work is coordinated today: the discovered-reviewer fan-out and its cost gate, sole-writer delegation enforced by `guard-adr.sh`, and the two purposes `disable-model-invocation` serves. Records three known gaps, including that the disposition of a discrepancy found during acceptance is only half-settled.
* **Creation**: established the [knowledge overview](knowledge/overview.md) — OKF as the format for every bundle, the regime's two tiers and four types, the ADR lifecycle, and the submodule/symlink route by which rules reach consumers.
* **Creation**: proposed [ADR-0005](knowledge/0005-ratify-adrs-in-concert-with-the-human.md) — ratification may amend the document it ratifies where the human directs it in session, keeping ADR-0004's guarantee that the human saw the text that froze while replacing the freeze that delivered it. An erratum on ADR-0004's "and nothing else" clause is owed when this decision takes effect, and is not yet written.
* **Creation**: proposed [ADR-0006](knowledge/0006-require-a-curator-session-for-adr-ratification.md) — admits the curator-session requirement the three human-authorisation gates already enforce and no decision had named. Raised by the pre-acceptance review of ADR-0005, which rests on it; the ledger entry follows at acceptance.
* **Creation**: established the [ADR lifecycle](knowledge/adr-lifecycle.md) specification, carrying the stage-by-stage contract and the amendment sequence that ADR-0005 and ADR-0006 keep out of their own bodies, plus the rule that an erratum is owed when a decision takes effect rather than when it is ratified.
* **Creation**: established the [components overview](components/overview.md) in a new `components/` theme — the skill/rule/agent/hook choice as a question of when content must be in context, the preloading cost model, and the rules-distribution workaround.
* **Creation**: established the [ticket-to-merge workflow](components/ticket-workflow.md) specification — how `/implement-ticket` and `/upstream` compose with the skills between them, and the sequencing the path imposes deliberately (no local reads before planning, `/plan-adr` never the entry point, acceptance decoupled from implementation). Records that only its review stage rests on a decision; the remainder is skill and rule text no ADR admits.
* **Update**: recorded the second rationale for the pre-planning read prohibition in the [ticket-to-merge workflow](components/ticket-workflow.md) — under a split-model configuration plan mode runs a more capable model, so the rule exists to cross that boundary before exploration rather than merely to avoid reads, which is why it is absolute rather than a preference.
* **Update**: recorded in the [knowledge overview](knowledge/overview.md) that the corpus postdates much of what it governs, so an unadmitted constraint here is as often settled-but-unrecorded as open — and that backfill happens on demand, in ADR-0006's shape, rather than as a migration.
* **Update**: reconciled the root index for the new theme, and the knowledge theme index for its overview, specification, and two proposed decisions.

## 2026-08-06

* **Acceptance**: marked [ADR-0004](knowledge/0004-split-architecture-documentation-into-immutable-adrs-and-living-documents.md) Accepted — ratified by `human:mscharley`. The errata sweep of prior accepted ADRs found none affected: ADR-0004's rename of `adr-architect` to `architecture-curator` is a nomenclature change, not a functional incorrectness, and ADR-0004 is a topology decision, so it admits no constraint-ledger entry.
* **Initialization**: adopted the architecture-documentation regime — moved the bundle from `docs/adr` to `docs/architecture`, scaffolded the root [Constraint Ledger](constraints.md) (seeded with the OKF knowledge-format commitment from [ADR-0002](knowledge/0002-adopt-okf-knowledge-bundles.md), refined to v0.2 by [ADR-0003](knowledge/0003-adopt-okf-v0-2-house-style.md)), retargeted the root index to the themes and the ledger, and rewrote the sole inbound cross-bundle link (the `okf-guide` skill's reference to ADR-0003).
* **Update**: refined [ADR-0004](knowledge/0004-split-architecture-documentation-into-immutable-adrs-and-living-documents.md)'s Acceptance wording — the gate ratifies the document as written, changing only status and verification, so the reviewed text is exactly what freezes.
* **Update**: refined [ADR-0004](knowledge/0004-split-architecture-documentation-into-immutable-adrs-and-living-documents.md)'s Adoption wording — a pre-OKF corpus stops adoption cleanly with direction to run `/okf-migrate-adr` first, rather than one human-gated migration firing another.
* **Creation**: proposed [ADR-0004](knowledge/0004-split-architecture-documentation-into-immutable-adrs-and-living-documents.md) — split architecture documentation into immutable ADRs (constraint admissions and topology decisions, named as commitments) and living documents (overviews, specifications, and a root constraint ledger that doubles as the regime marker) in per-project `docs/architecture` bundles; adds a shared materiality-gate skill, renames `adr-architect` to `architecture-curator`, distributes a generalised `architecture-reviewer`, and turns `/accept-adr` into a human ratification gate writing `verified`.
* **Update**: added the missing [ADR-0003](knowledge/0003-adopt-okf-v0-2-house-style.md) bullet to the knowledge theme index while reconciling it for ADR-0004.

## 2026-07-31

* **Acceptance**: marked [ADR-0003](knowledge/0003-adopt-okf-v0-2-house-style.md) Accepted once the `okf-*` skills and the adr-architect OKF-mode guidance were updated to the v0.2 house style.
* **Update**: added an erratum to [ADR-0002](knowledge/0002-adopt-okf-knowledge-bundles.md) recording that its v0.1 five-key / `# Citations` house style is superseded by [ADR-0003](knowledge/0003-adopt-okf-v0-2-house-style.md).
* **Creation**: proposed [ADR-0003](knowledge/0003-adopt-okf-v0-2-house-style.md) — migrate the Zantarix OKF house style to the v0.2 canonical form (`generated`, `sources`, trust/lifecycle families) across every bundle including ADRs, with ADRs keeping the mid-document `## References` citation section as the sole divergence; the `docs/adr` bundle marker bumps to `okf_version: "0.2"`.

## 2026-07-24

* **Initialization**: migrated the legacy flat ADR library into an OKF bundle per [ADR-0002](knowledge/0002-adopt-okf-knowledge-bundles.md) — themed subdirectories (`orchestration/`, `knowledge/`), four-digit numbering, frontmatter on every ADR, and this index/log pair replacing the `README.md` table and agent-private inventory.
